# ╔══════════════════════════════════════════════════╗
# ║  Aegis Hotspot Vault — Python Dependencies       ║
# ║  Author: Aryaditya Deshmukh (23BCE5056)          ║
# ╚══════════════════════════════════════════════════╝

# Cryptography
pycryptodome>=3.20.0          # RSA-2048 OAEP + AES-256-GCM

# Frontend dashboard (laptop only)
streamlit>=1.35.0             # Web-based Streamlit UI

# FTP server (laptop only)
pyftpdlib>=1.5.9              # Lightweight async FTP server

# ── Termux / Mobile (install separately in Termux) ─────────────
# pkg install python
# pip install pycryptodome
# (no streamlit or pyftpdlib needed on mobile)
